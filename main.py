"""Entry point — starts the ADK agent CLI session.

Sprint 1d. Loads .env (raising if ADK_API_KEY is missing), runs the read ->
sanitize -> agent -> print loop, auto-saves plans, and clears session state on
exit.
"""


def main() -> None:
    raise NotImplementedError("Implemented in Sprint 1d")


if __name__ == "__main__":
    main()
