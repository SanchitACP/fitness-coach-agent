"""Root ADK agent + sub-agent definitions.

Sprint 1d. Defines the Google ADK 2.0 root agent (Claude as the LLM backend)
that orchestrates the meal-planner and workout-selector sub-agents and registers
the three skills as FunctionTool objects.
"""

# Implemented in Sprint 1d. See product spec Section 5 for the agent definition
# and system-prompt template.
